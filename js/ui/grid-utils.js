// Grid utility functions
function updateGridSize(x, y, width, height) {
    const grid = document.getElementById('grid');
    if (!grid) return;
    
    // Grid is fixed at 2000x2000, no expansion needed
}
